Usage
=====

To run the analysis, execute the main script:

.. code-block:: bash

   python src/FinalProject.py

This will load the data, build the graph, and generate visualizations.

The data should be placed in the `data/` directory.

The output will be saved in the `output/` directory.
